@lumen 1.0
use sensitive.transfer

action critical(amount):
  reversible: 1h
  audit: full
  escalation: webhook(url="https://approvals.example.com", timeout=300s)
  execute:
    transfer.money(from=treasury, to=ops, amount=amount) because "Monthly ops"
